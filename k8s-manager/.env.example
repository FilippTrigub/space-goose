# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/
MONGO_DB=k8s_manager

# Kubernetes Configuration (choose one method)
# Method 1: Base64-encoded kubeconfig (recommended for deployment)
# KUBECONFIG_BASE64=<base64-encoded-kubeconfig-content>

# Method 2: Path to kubeconfig file (for local development)
# KUBECONFIG=/path/to/your/kubeconfig

# Method 3: Use default ~/.kube/config (no env var needed)

# Container Registry
ACR_IMAGE=your-registry.azurecr.io/goose-api:latest

# Application Settings
APP_HOST=0.0.0.0
APP_PORT=8000
APP_DEBUG=false

# Example: Convert kubeconfig to base64
# cat ~/.kube/config | base64 -w 0
# Then set KUBECONFIG_BASE64 to the output
